interface:
  display_name: "loop-arch"
  short_description: "Choose a minimal architecture slice before coding"
  default_prompt: "Inspect project state and `.agents/loop/todo.md`, resolve ambiguity or backlog gaps, update goals and implementation-ready todo entries, prepare a narrow handoff for `loop-build`, and call `loop-git` only for planning-state changes."
