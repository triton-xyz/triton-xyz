interface:
  display_name: "loop-git"
  short_description: "Stage, commit, and push one round slice"
  default_prompt: "Inspect git state, stage only current-round files, commit once, push to upstream or origin, and report commit and push results with exception handling."
